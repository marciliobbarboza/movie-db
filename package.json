{
  "name": "movies-api",
  "version": "0.1.0",
  "private": true,
  "main": "src/server.js",
  "scripts": {
    "dev": "npm --prefix ./apps/api run dev",
    "start": "npm --prefix ./apps/api run start",
    "front": "npm --prefix ./apps/web run dev",
    "frontend": "npm --prefix ./apps/web run dev",
    "db:sync-indexes": "npm --prefix ./apps/api run db:sync-indexes",
    "seed:tmdb": "npm --prefix ./apps/api run seed:tmdb"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.6.4",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}